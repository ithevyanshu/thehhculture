# Base URL of the DHH API.
# Leave unset in development: Vite proxies /api to http://localhost:4000.
# In production point it at your deployed backend, e.g.:
# VITE_API_URL=https://api.your-domain.com/api/v1
